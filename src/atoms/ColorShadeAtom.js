import { atom } from "recoil";

// Color Shade one
export const colorShadeOneAtom = atom({
  key: "colorShadeOneAtom",
  default: "#eee",
});
// Color Shade two
export const colorShadeTwoAtom = atom({
  key: "colorShadeTwoAtom",
  default: "#111",
});
