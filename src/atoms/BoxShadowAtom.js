import { atom } from "recoil";

// Width Atoms
export const valueWidthAtom = atom({
  key: "valueWidthAtom",
  default: 450,
});
// Height Atoms
export const valueHeightAtom = atom({
  key: "valueHeightAtom",
  default: 450,
});
